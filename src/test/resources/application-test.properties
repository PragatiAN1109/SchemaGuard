# Test profile configuration.
# Overrides the default 'redis' profile — tests always use InMemoryKeyValueStore.
# No Redis connection required to run the test suite.
spring.profiles.active=test
server.port=0
