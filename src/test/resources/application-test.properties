# Test profile configuration.
# Tests always use InMemoryKeyValueStore — no Redis, no Google token required.
# Security auto-configuration is NOT excluded here; instead the real
# SecurityFilterChain is loaded and @WithMockUser on each test class supplies
# a mock authenticated principal so no real JWT is needed.
server.port=0
google.client-id=test-client-id
