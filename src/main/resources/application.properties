# Default active profile — Redis is the primary store for demo and runtime.
# Override by setting SPRING_PROFILES_ACTIVE=test for the test suite.
spring.profiles.active=redis

# Server
server.port=8080
